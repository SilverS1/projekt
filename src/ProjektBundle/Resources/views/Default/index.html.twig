{% for projekt in projekts %}
	<h1>{{ projekt.progress }}</h1>
	<h1><a href="{{ path('projekt_show', { 'id': projekt.id }) }}">{{ projekt.name }}</a></h1>
	<h1>{{ projekt.id }}</h1>
{# 
	{% if projekt.mains %} 
		<h1>Hello world</h1>
	{% endif %} #}
{% endfor %}


{# {% for main in mains %}
	<h1>{{ main.name }}</h1>
{% endfor %} #}

{# {% for main in mains2 %}
	<h1>{{ main.name }}</h1>
{% endfor %}
 #}